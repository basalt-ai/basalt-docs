import { Libraries } from '@/components/Libraries'

export const metadata = {
  title: 'Prompts',
  description:
    'Basalt offers officially supported Python and JavaScript/TypeScript SDKs to provide you with the best possible developer experience when integrating with our API.',
}

export const sections = [
  { title: 'Retrieve a Prompt', id: 'retrieve-a-prompt' },
  { title: 'Returned data', id: 'returned-data' },
  { title: 'Get a specific Version', id: 'specific-version' },
  { title: 'Get a specific Tag', id: 'specific-tag' },
  { title: 'Replace variables', id: 'replace-variables' },
  { title: 'Cache policy', id: 'cache-policy' },
  { title: 'Error handling', id: 'error-handling' }
]

# Prompts

Prompts are a fundamental part of the system, allowing you to maintain and version control your AI prompt templates. On this page, we'll explore how to retrieve prompts and their specific versions programmatically. We'll look at how to fetch prompts using their unique identifiers and how version resolution works.


## Retrieve a Prompt

<CodeGroup>
```ts
// Import the Basalt SDK
import Basalt from '@basalt-ai/sdk'

// Initialize the SDK with your API key
const basalt = new Basalt({ apiKey: "YOUR_API_KEY" })
```

```python
# Import the Basalt SDK
from basalt import Basalt

# Initialize the SDK with your API key
basalt = Basalt('YOUR_API_KEY')
```

</CodeGroup>

## Returned data


## Get a specific Version


## Get a specific Tag


## Replace variables

## Cache policy

## Error handling